{{ format.name }} (np.{{ dtype }}, {{ "Planar" if format.is_planar else "Packed" }})

Supported Decoders:
{%- set codecs = decoders | sort(attribute="name") %}
{%- for codec in codecs %}
  - {{ codec.canonical_name }} ({{ codec.long_name }})
{%- endfor %}

Supported Encoders:
{%- set codecs = encoders | sort(attribute="name") %}
{%- for codec in codecs %}
  - {{ codec.name }} ({{ codec.long_name }})
{%- endfor %}
